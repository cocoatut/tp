import React from 'react';

const Image = () => {
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        height: '90vh'
      }}
    >
      <h1>image</h1>
    </div>
  );
};

export default Image;